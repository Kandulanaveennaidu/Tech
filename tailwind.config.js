module.exports = {
  mode: 'jit',
  // other configurations...
  theme: {
    extend: {
      colors: {
        // black: '#000000',
      },
      fontFamily: {
        duera: ['Duera', 'sans'], // Replace 'Duera' with your desired font name
      },
    },
  },
  plugins: [
    // Add any additional plugins here
  ],
};
